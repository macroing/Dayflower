Geometry API:

	* ADD:		Add method contains(Point3D) to Shape3D.
	* ADD:		Add method contains(Point3F) to Shape3F.

Geometry Shape API:

	* ADD:		LineSegment3D.
	* ADD:		LineSegment3DReader.
	* ADD:		LineSegment3FReader.
	* ADD:		Polygon3D.
	* ADD:		Polygon3DReader.
	* ADD:		Polygon3FReader.
	* CHANGE:	Implement Polygon3F.
	* CHANGE:	Implement LineSegment3F.

Image API:

	* ADD:		Add the constructor ByteImageD(BufferedImage) to the ByteImageD class.
	* ADD:		Add the constructor ByteImageF(BufferedImage) to the ByteImageF class.
	* ADD:		Add the constructor DoubleImageD(BufferedImage) to the DoubleImageD class.
	* ADD:		Add the constructor FloatImageF(BufferedImage) to the FloatImageF class.
	* ADD:		Add the methods createScreenCapture(Rectangle2I), difference(ImageD, ImageD), load(File), load(String) and load(URL) to the ByteImageD class.
	* ADD:		Add the methods createScreenCapture(Rectangle2I), difference(ImageF, ImageF), load(File), load(String) and load(URL) to the ByteImageF class.
	* ADD:		Add the methods createScreenCapture(Rectangle2I), difference(ImageD, ImageD), load(File), load(String) and load(URL) to the DoubleImageD class.
	* ADD:		Add the methods createScreenCapture(Rectangle2I), difference(ImageF, ImageF), load(File), load(String) and load(URL) to the FloatImageF class.

JavaFX Application API:

	* ADD:		Add support for FileParameter in the ParameterLoader implementation.

Parameter API:

	* ADD:		Add support for different Parameter implementations that contains lists of options.

Renderer GPU API:

	* ADD:		Add support for DiffuseAreaLight.
	* ADD:		Add support for PrimitiveAreaLight.
	* ADD:		Add support for UberMaterial.
	* ADD:		Add support for LDRImageNormalMapModifier.
	* ADD:		Add support for SimplexNoiseNormalMapModifier.
	* ADD:		Add support for ConstructiveSolidGeometry3F.
	* ADD:		Add support for the Ray Tracer algorithm.
	* ADD:		Add support for the Scene-based BVH structure.
	* FIX:		Fix the bug that resets the image occasionally using the Ambient Occlusion and Path Tracer algorithms.
	* FIX:		Fix rendering of transmissive materials. Mainly in the Path Tracer.

Scene BSSRDF API:

	* ADD:		Add implementations.

Scene Compiler API:

	* ADD:		Add support for PrimitiveAreaLight.
	* ADD:		Add support for UberMaterial.
	* ADD:		Add support for LDRImageNormalMapModifier.
	* ADD:		Add support for SimplexNoiseNormalMapModifier.
	* ADD:		Add support for ConstructiveSolidGeometry3F.
	* ADD:		Add support for updating a CompiledScene instance on-the-fly.

Scene Light API:

	* ADD:		Add an HDRImageLight class.

Scene Material API:

	* ADD:		Add a Builder to the HairMaterial class.
	* ADD:		Add a Builder to the MetalMaterial class.
	* ADD:		Add a Builder to the SubstrateMaterial class.
	* ADD:		Add a Builder to the UberMaterial class.
	* FIX:		Fix the HairMaterial class.

Scene Texture API:

	* ADD:		Add an HDRImageTexture class.

Utility API:

	* ADD:		Add the class LongArrays.
	* ADD:		Add methods to convert from byte[] to double[], float[] int[], and long[] in the ByteArrays class.
	* ADD:		Add methods to convert from double[] to byte[], float[], int[] and long[] in the DoubleArrays class.
	* ADD:		Add methods to convert from float[] to byte[], double[], int[] and long[] in the FloatArrays class.
	* ADD:		Add methods to convert from int[] to byte[], double[], float[] and long[] in the IntArrays class.
	* ADD:		Add methods to convert from long[] to byte[], double[], float[] and int[] in the LongArrays class.
	* CHANGE:	Move array creation methods from the class Bytes to the class ByteArrays.
	* CHANGE:	Move array creation methods from the class Doubles to the class DoubleArrays.
	* CHANGE:	Move array creation methods from the class Floats to the class FloatArrays.
	* CHANGE:	Move array creation methods from the class Ints to the class IntArrays.
	* CHANGE:	Move array creation methods from the class Longs to the class LongArrays.