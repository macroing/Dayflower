Parameter API:

	* ADD:		Add support for different Parameter implementations that contains lists of options.

Renderer GPU API:

	* ADD:		Add support for DiffuseAreaLight.
	* ADD:		Add support for PrimitiveAreaLight.
	* ADD:		Add support for UberMaterial.
	* ADD:		Add support for LDRImageNormalMapModifier.
	* ADD:		Add support for SimplexNoiseNormalMapModifier.
	* ADD:		Add support for ConstructiveSolidGeometry3F.
	* ADD:		Add support for the Ray Tracer algorithm.
	* ADD:		Add support for the Scene-based BVH structure.
	* FIX:		Fix the bug that resets the image occasionally using the Ambient Occlusion and Path Tracer algorithms.
	* FIX:		Fix rendering of transmissive materials. Mainly in the Path Tracer.

Scene BSSRDF API:

	* ADD:		Add implementations.

Scene Compiler API:

	* ADD:		Add support for updating a CompiledScene instance on-the-fly.
	* CHANGE:	Move the implementations of the methods toShape3F*Array() from the Shape3FCache class to the CompiledShape3FCache class. The methods should be toShape3F*Array(List<*>) and toShape3F*Array(List<*>, ToIntFunction<BoundingVolume3F>).
	* CHANGE:	Move the implementations of the methods toTexture*TextureArray() from the TextureCache class to the CompiledTextureCache class. The methods should be toTexture*TextureArray(List<*>) and toTexture*TextureArray(List<*>, ToIntFunction<Texture>).
	* CHANGE:	Change the naming convention of the CompiledShape3FCache class. The methods should be similar to getSphere3FArray() instead of getShape3FSphere3FArray() etc. Similar with fields.
	* CHANGE:	Change the naming convention of the CompiledTextureCache class. The methods should be similar to getBlendTextureArray() instead of getTextureBlendTextureArray() etc. Similar with fields.
	* CHANGE:	Change the method names from something like toArray(Sphere3F) to toSphere3FArray(Sphere3F). Similar to the methods that take a List<Sphere3F> etc.

Scene Light API:

	* ADD:		Add an HDRImageLight class.

Scene Material API:

	* ADD:		Add a Builder to the HairMaterial class.
	* ADD:		Add a Builder to the MetalMaterial class.
	* ADD:		Add a Builder to the SubstrateMaterial class.
	* ADD:		Add a Builder to the UberMaterial class.
	* FIX:		Fix the HairMaterial class.

Scene Texture API:

	* ADD:		Add an HDRImageTexture class.